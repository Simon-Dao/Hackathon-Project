var dayNumber = 0
var dayChangeButton = document.querySelector('#day-button')
function incrementDay() {
    dayNumber++
}

/*
dayChangeButton.addEventListener('click', (e) => {
    e.preventDefault()
    incrementDay();
    console.log(dayNumber)
})*/